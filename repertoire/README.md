# Repertoire

Generated by [`scripts/repertoire/`](../scripts/repertoire/) — design in ADR
[0021](../docs/decisions/0021-opening-repertoire-generator.md), depth in
[0025](../docs/decisions/0025-curated-lines-run-to-the-structure.md), the
evaluation gate in
[0024](../docs/decisions/0024-gate-on-a-local-evaluation-index.md).

## Use [v2](v2/) — two files

Import `v2/etude-white-standard.pgn` and `v2/etude-black-standard.pgn` into En Croissant
(Files → Add New → from PGN), **separately**, then point the repertoire trainer
at whichever side you are drilling. Move up to `-complete` when that becomes automatic. Four files exist; two are ever in use.

Everything else about the tiers is in [v2/README.md](v2/README.md), including
the two things to know before you drill. The moves are gated at depth 34–50 and
sound. And the trap annotations have been **replicated** — `replicate.mjs` ran
June 2026 against July 2026, so a `· trap ·` comment means two independent
months agreed on the finding *and* its magnitude (98 of 149 for 1.d4 + Black,
184 of 217 for 1.e4). Anything only one month saw now reads `· one month only ·`
instead: its statistics still stand, but a `?` there is not a refutation.

[v1/](v1/) is the 2026-08-07 generation, kept for comparison. It is shallower
(deepest ply 13 against v2's 19–20) and was built from a 367k-game band book
against v2's 8M. Nothing needs it except a diff.

## Rebuilding

Needs the databases under `db/`, which are gitignored and must be re-fetched —
see [scripts/repertoire/README.md](../scripts/repertoire/README.md).

```
npm run rep:build       # the 1.d4 + Black deck, ~4h
npm run rep:build:e4    # the 1.e4 deck
npm run rep:decks       # re-stage the tiers into v2/
npm run rep:audit       # re-grade every prescribed move against the index
npm run rep:study       # what to learn first, ranked
```
